# Claude Entrypoint

Read `AGENTS.md` at session start. It is the canonical context for this repo,
shared across Claude Code and Codex.

Claude-specific local configuration may remain under `.claude/`.
