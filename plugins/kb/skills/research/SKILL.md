---
name: research
description: Deprecated kb compatibility alias for Memento research. Check existing research before doing new work, then update or create sourced research docs.
user-invocable: false
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Agent]
---

# Research (Deprecated KB Alias)

`kb` has been renamed to **Memento**. This compatibility skill preserves the
old research workflow for existing `kb` installs.

Use the canonical workflow in:

`../../../memento/skills/research/SKILL.md`

Follow that file exactly. Keep old `KB_ROOT` and `.kb-root` configurations
working through the compatibility root scripts, but prefer `MEMENTO_ROOT` and
`.memento-root` in new guidance.
