---
name: remember
description: Deprecated kb compatibility alias for Memento remember. Additive capture that writes source material only under sources.
argument-hint: "<thing to remember>"
user-invocable: true
allowed-tools: [Read, Write, Glob, Bash]
---

# Remember (Deprecated KB Alias)

`kb` has been renamed to **Memento**. Use the canonical workflow in:

`../../../memento/skills/remember/SKILL.md`

Keep old `KB_ROOT` and `.kb-root` configurations working, but prefer
`MEMENTO_ROOT` and `.memento-root` in new guidance.
