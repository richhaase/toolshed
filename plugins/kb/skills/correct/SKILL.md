---
name: correct
description: Deprecated kb compatibility alias for Memento correct. Supersede or archive stale source material while preserving history.
argument-hint: "<source or topic>"
user-invocable: true
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash]
---

# Correct (Deprecated KB Alias)

`kb` has been renamed to **Memento**. Use the canonical workflow in:

`../../../memento/skills/correct/SKILL.md`

Keep old `KB_ROOT` and `.kb-root` configurations working, but prefer
`MEMENTO_ROOT` and `.memento-root` in new guidance.
